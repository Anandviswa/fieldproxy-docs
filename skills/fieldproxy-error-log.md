# FieldProxy Error Log

> Track bugs found during view generation. Each entry feeds back into skill updates.

## How to Use

When a generated view has a bug:
1. Add a row below with the date, view, symptom, root cause, and which skill was updated
2. If a new rule is needed, add it to the relevant skill file
3. If an existing rule was insufficient, update it with the new edge case

---

## Error Log

| Date | Project | View | Symptom | Root Cause | Skill Updated |
|------|---------|------|---------|------------|---------------|
| 2026-03-04 | Safegroup | enquiry_detail | Empty state always visible | `show_if` not a valid property — must use `visibility_condition` | widget-rules: Rule 11 added |
| 2026-03-04 | Safegroup | enquiry_detail | Template values failing in visibility conditions | Unquoted `{{...}}` in conditions — must wrap in single quotes | widget-rules: Rule 12 added |
| 2026-03-04 | Safegroup | enquiry_detail | Button too wide / ignoring size | `fixed_width` not valid for buttons — must use grid `width` 1-12 | widget-rules: Rule 13 added |
| 2026-03-04 | Safegroup | enquiry_detail | Input silently disappearing | `textarea` / `select` not valid input_types — use `text` + `max_lines` / `dropdown` | widget-rules: Rule 14 added |
| 2026-03-04 | Safegroup | enquiry_detail | Wrong input value read in popup | `.[0]` vs `.0` confusion — brackets cause integer index, no brackets = string key | widget-rules: Rule 15 added |
| 2026-03-04 | Safegroup | create_quote | Screen goes blank when clicking "Add Line Item" button | Popup defined as top-level `ui[]` element referenced via `navigation_type: "popup"` + `popup_id` — FieldProxy requires popups to be **inline** inside the button's `onclick.1` with `container_with_title` + `actions` array pattern. Cancel/close must use `"back": true, "type": "navigation"`, not `"type": "close_popup"` | input-forms / widget-rules: popup pattern clarified |
| 2026-03-05 | Safegroup | create_quote | INSERT/UPDATE in `data` query silently fails — no draft created | `data` object queries are **SELECT-only**. Any INSERT/UPDATE/DELETE inside a `data` query is silently ignored. Must use `execute_query` inside an `onclick` action instead. | architecture: data queries are read-only |
| 2026-03-05 | Safegroup | create_quote | Draft quote not available when form view loads | Cannot create a record on the destination view's load. Must **pre-create** the draft via `execute_query` in the **source view's** button `onclick` (before `navigation`), then the destination view's `data` query SELECTs the newly created row. | architecture: pre-create on navigation pattern |
| 2026-03-05 | Safegroup | create_quote | Line Items table columns squished — headers & data run together with no spacing | `width` property on `text` widgets inside a `row` is **ignored** — text renders at natural width. Must wrap each `text` in a `container` with `width` to create proper table columns. Container respects width in row grid layout; text does not. | widget-rules: width on text in row |
| 2026-03-05 | Safegroup | quote_detail | Cards collapse to content width instead of full width | Container cards inside a page body need explicit `"width": 12` to fill the 12-column grid. Without it, containers shrink-wrap to their content. | widget-rules: always set width on card containers |
| 2026-03-05 | Safegroup | quote_detail | Label/value fields in a row run together — "SGQ-2026-00605 Mar 2026..." | `column` widgets inside a `row` render edge-to-edge with no gap. Must add `{"type": "gutter", "element_id": "..."}` between each `column` in a `row` to get spacing. | widget-rules: gutter between row columns |
| 2026-03-05 | Safegroup | quote_detail | Status badge text invisible (empty pill) | `qs.color` from `quote_statuses` table was NULL for some status rows. Status color in template `"color": "{{...status_color}}"` rendered as empty/null. Fix: use `COALESCE(qs.color, '#6B7280')` in query so there's always a fallback color. | architecture: always COALESCE color fields |
| 2026-03-05 | Safegroup | quote_detail | Line items empty on detail view despite being added during creation | The Save Draft `onclick` flow moves line items from `draft_line_items` to `quote_line_items` using a subquery: `(SELECT id FROM quotes WHERE prepared_by = '...' ORDER BY created_at DESC LIMIT 1)`. If the INSERT into quotes hasn't committed yet or the subquery returns wrong ID, line items get orphaned. **Verify**: check `quote_line_items` table directly for the quote_id. | architecture: verify subquery returns correct ID in sequential actions |
| 2026-03-05 | Safegroup | quote_detail | **UI Upgrade** — Header converted from plain row to white container banner with border/radius. Flat "Quote Information", "Customer & Site", and "Notes to Customer" cards replaced with: (1) **Top summary card** — title + two side-by-side boxes (Notes to Customer on gray #F9FAFB bg, Financial Summary on blue #EFF6FF bg with subtotal/VAT/total breakdown). (2) **3-column info grid** — Quote Details \| Customer & Site \| Status & Tracking with icons, sub-labels, badges, and vertical dividers (pattern from enquiry_detail). Line items table header wrapped in gray #F9FAFB container. Data query updated to include `sent_at_display`. Terms card given `line_height: 1.6`. | UI upgrade — no skill update needed |
| 2026-03-05 | Safegroup | quote_detail | Totals section — labels and values squished together with no spacing; totals left-aligned instead of right | `spacer` widget inside a `row` that is inside a `column` (width:5) does not expand. Fix: replaced `spacer` pattern with explicit container-wrapped columns (`width: 6` each) for label and value, with `text_align: "right"` on value containers. Also replaced `spacer` for left push with empty `container` (width:7). Added blue `#EFF6FF` highlight bg on total row for emphasis. | widget-rules: spacer unreliable inside nested row/column — use container widths + text_align instead |
| 2026-03-05 | Safegroup | quote_detail | Terms & Conditions card too plain — just raw text | Added icon + header row (`shield` icon), wrapped terms text in `#F9FAFB` tinted container with border-radius and padding for visual separation. | UI improvement — no skill update |
| 2026-03-05 | Safegroup | quote_detail | Activity Log visually poor — flat gray boxes with no hierarchy | Redesigned with timeline-style layout: blue dot icon in `#EFF6FF` circle on left, action type as blue `#EFF6FF` badge, notes as body text, user/date as subtle row with `user` and `clock` micro-icons. Each entry has a subtle border card. | UI improvement — no skill update |
| 2026-03-06 | Safegroup | n8n workflow | Character encoding mojibake — smart quotes, pound signs, dashes appearing as garbled Latin-1 characters (e.g., `\u00E2\u0080\u0099` instead of `'`) | Outlook emails encoded in Windows-1252 charset; IMAP library mis-decodes UTF-8 multi-byte chars as Latin-1. Fix: `fixEncoding()` function in Strip Email Signature — tries `Buffer.from(text, 'latin1').toString('utf8')` round-trip, falls back to known mojibake pattern replacement. | n8n workflow: encoding repair in inb-03 |
| 2026-03-06 | Safegroup | n8n workflow | `<mailto:john@example.com>` artifacts appearing in parsed thread message bodies | Outlook Rich Text format inserts `<mailto:...>` hyperlinks inline in the `textPlain` body next to email addresses. These survived into parsed thread messages. Fix: `threadText = threadText.replace(/<mailto:[^>]*>/g, '')` pre-clean step in Parse Email Thread before regex parsing. | n8n workflow: pre-clean in inb-03a |
| 2026-03-06 | Safegroup | n8n workflow | Apple Mail / Gmail thread messages not being parsed — only Outlook format detected | Initial regex only matched `On DATE, NAME <email> wrote:` but Apple Mail uses `On DATE, at TIME, NAME <email> wrote:` (with an extra `at TIME` component). Fix: Updated Apple Mail regex to `On\\s+(.+?),\\s+(?:at\\s+[\\d:]+,\\s+)?(.+?)\\s+<([^>]+)>\\s+wrote:` making the `at TIME` portion optional. | n8n workflow: Apple Mail regex in inb-03a |
| 2026-03-06 | Safegroup | n8n workflow | ALL thread history messages showing as "Inbound" in conversation view — 22 Inbound, 0 Outbound | Build Thread History SQL inserted ALL historical messages into `enquiry_emails` table. The conversation view UI determines direction by **source table** (`enquiry_emails` = inbound, `enquiry_replies` = outbound), not by a direction column. Fix: Split inserts by sender domain — outbound messages (from OUR_DOMAINS) go to `enquiry_replies`, inbound to `enquiry_emails`. | n8n workflow: direction routing in inb-19a |
| 2026-03-06 | Safegroup | enquiry_detail | Messages not in correct chronological order in conversation view | `conversation_thread` query sorted by `sent_at_formatted` — a display string like `'↓ 02 Mar 2026, 09:30'`. String sort on `DD Mon YYYY` format is NOT chronological (e.g., "02 Mar" < "14 Aug" alphabetically, but "02 Dec" < "02 Mar"). Fix: Added `sort_date` column (actual `TIMESTAMPTZ`) and changed `ORDER BY sort_date ASC NULLS LAST`. | enquiry_detail_view: conversation_thread query |
| 2026-03-06 | Safegroup | enquiry_detail | Outbound message sender initial always showing 'S' instead of actual staff name | `sender_initial` was hardcoded as `'S'` in the outbound UNION branch of `conversation_thread` query. Fix: Changed to `UPPER(LEFT(COALESCE(er.sent_by,'S'),1))` to use actual staff name from `enquiry_replies.sent_by`. | enquiry_detail_view: conversation_thread query |
| 2026-03-06 | Safegroup | n8n workflow | Duplicate outbound messages — each outbound message appearing twice (32 Total: 12 Inbound, 20 Outbound when expected ~16) | In nested Outlook thread quoting, the same `From:/Sent:` header block appears at MULTIPLE quoting levels (each reply includes the full prior thread). This produced duplicate boundary detections in Parse Email Thread. Fix: Added boundary-level deduplication using `sender_email + '\|' + sent_date` as composite key, keeping only the FIRST (outermost) occurrence which has the cleanest body text. | n8n workflow: boundary dedup in inb-03a |
| 2026-03-06 | Safegroup | n8n workflow | `ON CONFLICT (outbound_message_id) DO NOTHING` fails with "no unique or exclusion constraint matching" | `enquiry_replies` table has only a regular INDEX on `outbound_message_id` (`idx_enquiry_replies_outbound_message_id`), NOT a UNIQUE constraint. `ON CONFLICT` requires a UNIQUE index/constraint. Fix: Replaced bulk INSERT with individual `INSERT INTO ... SELECT ... WHERE NOT EXISTS (SELECT 1 FROM enquiry_replies WHERE outbound_message_id = X AND enquiry_id = Y)` statements. | n8n workflow: NOT EXISTS guard in inb-19a |
| 2026-03-06 | Safegroup | n8n workflow | `CREATE UNIQUE INDEX` on `outbound_message_id` fails with HTTP 400 | Existing duplicate `outbound_message_id` values in the table from prior workflow runs prevented creating a UNIQUE index. Would require deleting duplicates first. Decided NOT to add unique index — used `NOT EXISTS` guard instead which works without schema changes. | n8n workflow: schema-free dedup approach |
| 2026-03-06 | Safegroup | n8n workflow | Match Customer node returning `undefined` for `customer_id` when no customer matched | The UNION query with `LIMIT 1` on the outer wrapper returned empty result set when neither email domain nor postcode matched. The `COALESCE(matched.id, 0)` only works if at least one row exists. Fix: Query already had proper COALESCE wrapping — issue was the n8n expression `$('Match Customer').item.json.customer_id` returning undefined when the query returned 0 rows. Ensured query always returns exactly one row. | n8n workflow: Match Customer query in inb-16 |
| 2026-03-06 | Safegroup | n8n workflow | `Variable $773671 exceeds supported maximum of $100000` — Check Duplicate node crashes | Message-ID `012c01dcad65$27bcd0a0$773671e0$@traceheat.co.uk` contains `$` followed by digits. The node-postgres (pg) library interprets `$773671` as a query parameter placeholder (like `$1`, `$2`). Since 773671 > 100000 (pg max), it throws an error. This affects ALL queries where message IDs or email content containing `$` + digits are interpolated into SQL. Fix: Updated `sqlEsc()` in all 4 Code nodes to also replace `$` with `' \|\| CHR(36) \|\| '` — PostgreSQL's `CHR(36)` function returns `$`, so the concatenation produces the correct string, but pg never sees `$N` patterns. Also changed `_inReplyTo`/`_threadRootId` references to use SQL-escaped `_sql.*` versions, and wrapped `::JSONB` casts in parentheses so concatenation evaluates before casting. | n8n workflow: sqlEsc `$` escape + JSONB parens in all nodes |
| 2026-03-08 | Waterade | service_detail | Machine row columns (status badges, action buttons) not visible — only serial number and "Pending" text showing | **Three compounding issues:** (1) Children column widths totaled 12 (100% screenWidth) inside a `width: 7.5` parent container. (2) `mach_info_col` was `type: "column"` and `mach_actions_row` was `type: "row"` — per Rule 1, these types ignore their own `width` property. Fix: Wrapped in containers. (3) **Source code confirmed**: `calculateDynamicWidth()` in `container_widget.dart` ALWAYS uses `widget.controller.screenWidth` — never parent constraints. So children inside a padded parent container (width 7.5 + 48px padding) overflow when `sum_of_children_widths > parent_width - (48*12/screenWidth)`. A `type: "row"` (Flutter Row) clips overflow silently. Fix: Changed `mach_data_row` to `type: "wrap"` — Flutter Wrap never clips; it wraps to next line instead. | widget-rules: Rule 2 updated + Rule 16 added (use wrap for nested multi-column layouts) |
| 2026-03-08 | Waterade | service_detail | Icon buttons invisible despite being interactive (tooltip shows on hover) | `btn_type: "icon_button"` renders as `CircleAvatar` + `IconButton`. Uses `bg_color` (not `background_color`) and `color` (not `icon_color`). Defaults are BOTH `#FFFFFF` — white icon on white background = invisible. Properties `background_color`, `icon_color`, `border_radius` are **silently ignored**. Fix: Changed to `bg_color`, `color`, `size`. | widget-rules: Rule 17 added (icon_button property names) |
| 2026-03-08 | Waterade | service_detail | List item element_ids with `_[index]` suffix — state collision + broken input_data access | `_[index]` in element_ids is NOT replaced by the framework — it stays as literal string. Framework differentiates rows via internal `$index` parameter. `_[index]` in field paths like `input_data.verify_notes_[index].0` causes `getNestedData` to try `int.parse("verify_notes_index")` → silently fails. Fix: Removed `_[index]` from all list item element_ids. Changed `input_data.X_[index].0` to `input_data.X.[index]` for list popup inputs. | widget-rules: Rule 18 added (list element_id pattern) |
| 2026-03-09 | Waterade | replacement_job | Dropdown selected value not passing to SQL — `replacement_machine_id` NULL in INSERT despite multiple fix attempts | **Root cause (confirmed)**: `input_data` is keyed by **`element_id`**, NOT `field_name`. All prior attempts used `field_name` (`rpl_machine`) as key — always null regardless of `option_value`/`value_field`/`store` config. **Fix**: Use `element_id` as the key. For object-data dropdown in list popup: `{{input_data.rpl_machine_input.[index].machine_id}}` — element_id + list index + field name. For plain text/date in list popup: `{{input_data.rpl_send_date_input.[index]}}`. Rule: ALWAYS use `element_id` (never `field_name`) in `input_data` template expressions. | widget-rules: Rule 19 added |
| 2026-03-09 | Waterade | replacement_job | Machine status stuck at `allocated` when saving assignment after job already exists | **Design flaw**: Save Assignment always set `machine_status = 'allocated'`. If user adds a machine AFTER the job was created, the machine never gets promoted to `replacement_scheduled`. Fix: Changed UPDATE to `CASE WHEN EXISTS (SELECT 1 FROM replacement_jobs WHERE service_ticket_id = {{arguments.ticket_id}} AND delete = false) THEN 'replacement_scheduled' ELSE 'allocated' END` — smart promotion based on whether a job already exists for this ticket. | status lifecycle: late assignments now correctly get replacement_scheduled |
| 2026-03-09 | Waterade | add_new_machine | INSERT query silently inserts NULL for all form fields — machine_number, serial_number, birth_date, comments all null | Missing `.0` suffix on all `input_data` template references in the `execute_query` action. Regular form view inputs (non-list context) must use `{{input_data.ELEMENT_ID.0}}` not `{{input_data.ELEMENT_ID}}`. Same applies to notification messages referencing form inputs. Fix: Added `.0` to all four references in INSERT query and notification message. | widget-rules: Rule 19 (non-list inputs require .0 suffix) |
| 2026-03-09 | Waterade | service_detail | Start Service sets machine_status = 'under_service' regardless of service type — two distinct statuses not reflected | Status color map already had `under_service_fridge` (Engine Service) and `under_service_standard` (Normal Service) but the UPDATE query hardcoded `'under_service'`. Fix: Changed to `CASE WHEN '{{input_data.svc_type_input.[index]}}' = 'Engine Service' THEN 'under_service_fridge' ELSE 'under_service_standard' END` so machine status correctly reflects the type of service being performed. | service_detail: query_svc_machine |
| 2026-03-12 | EFS | admin_supply_requests | Input widget silently disappears — search bar not visible | `input_type: "search"` is not a valid FieldProxy input type. Valid types: `text`, `number`, `email`, `phone`, `date`, `dropdown`, `checkbox`, `file`, `image`, `mcq`, `rating`, `signature`, `location`, `scan`, `step_counter`, `table`, `catalogue`. Fix: Changed to `"input_type": "text"`. | widget-rules: Rule 14 (repeat) |
| 2026-03-12 | EFS | admin_supply_requests + facilities_list | Icon buttons invisible — white icon on white background, properties silently ignored | `icon_button` uses `color`/`bg_color`/`size` — NOT `icon_color`/`background_color`/`border_radius`. Wrong properties are silently ignored, defaults are both `#FFFFFF`. 5 icon buttons affected across 2 screens. Fix: Changed all to `color`, `bg_color`, `size`. | widget-rules: Rule 17 (repeat — 5 instances) |
| 2026-03-12 | EFS | facilities_list | Sidebar navigation broken — clicking Supply Requests or Issues navigates to wrong/missing view | Sidebar `view_id` values didn't match actual screen `view_id`s. `supply_requests_view` vs actual `admin_supply_requests_view`, `issues_view` vs `admin_issues_view`, and self-reference mismatch. Also inconsistent sidebar header text ("EFS" vs "EFS Admin"). Fix: Aligned all sidebar nav `view_id`s to `admin_*` convention across screens. | architecture: cross-screen nav consistency |
| 2026-03-12 | EFS | facilities_list | Container background not showing — intended gray #F3F4F6 renders as white | Container widgets use `color` for background, not `background_color`. `background_color` is only valid inside sidebar items and `filled_button`. Fix: Changed `background_color` to `color` on container. | widget-rules: container `color` vs `background_color` |
| 2026-03-12 | EFS | cleaner_supply_requests + cleaner_report_issues | `expanded: true` on containers silently ignored — layout doesn't fill row | `expanded: true` only works on `type: "text"` widgets (Rule 4). On containers inside rows, it has no effect. Fix: Use explicit `width` values or restructure with `column` + `spacer`. | widget-rules: Rule 4 (repeat — 8 instances) |
| 2026-03-12 | EFS | seed vs UI | Seed data values don't match dropdown options — display/filter mismatch | Seed `cleaning_frequency = 'twice_weekly'` doesn't match dropdown options (`daily`, `weekly`, `bi-weekly`, `monthly`). Seed `supply_responsibility` uses long text strings instead of coded values (`company`, `client`, `shared`). Fix: Align seed data values to dropdown option values. | architecture: seed data must match dropdown options |
| 2026-03-12 | EFS | cleaner_supply_requests + cleaner_report_issues | `box_shadow` CSS string may not render — similar to CSS `border` issue (Rule 3) | `box_shadow: "0 2px 8px rgba(0,0,0,0.06)"` uses CSS syntax that FieldProxy renderer may not parse. Similar to how CSS `border` shorthand doesn't work. Needs verification against renderer. | widget-rules: pending verification |
| 2026-03-12 | EFS | cleaner_supply_requests + cleaner_report_issues | `width: 4000` used instead of grid system max `12` | Mobile buttons and empty state containers used `width: 4000` to force full width. Standard approach is `width: 12` for full-width elements in the 12-column grid. | widget-rules: use width 12, not arbitrary large numbers |
| 2026-03-12 | EFS | cleaner_supply_requests + cleaner_report_issues | `position: "fixed"` and `bottom: 24` — CSS positioning may not be supported | FAB buttons used CSS-like positioning properties that FieldProxy renderer may not support. Needs verification. | widget-rules: pending verification |
| 2026-03-12 | EFS | facilities_list | Icon "building" renders as default star icon | `"building"` is NOT a valid Feather icon name. FieldProxy uses the Feather icon set — unrecognized names render as a star (★) fallback. Valid alternatives for building/facility: `home`, `map-pin`, `briefcase`, `box`. Fix: Changed `"icon": "building"` to `"icon": "home"`. | widget-rules: Rule 20 added (valid Feather icon names) |
| 2026-03-13 | Safegroup | enquiry_detail | Duplicate `element_id`: `nav_after_archive` appears twice (lines ~220 and ~719) — second occurrence added during modification collided with pre-existing element | When modifying an existing file, new elements must be checked against ALL existing element_ids in the file, not just other new elements. The builder added a navigation action with `nav_after_archive` without noticing the same ID already existed in the original file. Fix: Rename second occurrence to `nav_after_archive_2`. | widget-rules: Rule 6 note added (check existing IDs when modifying files) |
| 2026-03-13 | Safegroup | enquiry_detail | CSS `border` shorthand on newly added `visits_banner_card`: `"border": "1px solid #E5E7EB"` — silently ignored by renderer | Rule 3 violation on a newly built element. Despite Rule 3 being documented since initial skill creation, the builder used CSS shorthand syntax. Additionally, 7 pre-existing CSS `border` strings found in the same file (tech debt). Fix: Replace with `"border_color": "#E5E7EB", "border_width": 1`. | widget-rules: Rule 3 (repeat — pre-flight checklist not loaded?) |
| 2026-03-12 | EFS | facilities_list | Icon wrapper container renders as oversized blue rectangle (~120px wide) instead of compact 40px square | Container with `width: 1` inside a list card row. `calculateDynamicWidth()` uses `screenWidth` (not parent), so `1/12 × 1440px = 120px` — far too large for a 36px icon. Fix: **Remove `width` property** from container. Inside a `row` with `main_axis_size: "min"`, a container without explicit `width` shrink-wraps to its content (icon + padding). | widget-rules: Rule 21 added (small containers in rows) |
| 2026-03-12 | EFS | facilities_list | Facility name/sqft text overlaps with address column — all detail columns run together | Three nested inner rows (identity, details, actions) inside one parent `row` with `space_between`. Each inner row's children use grid widths (2.5, 1.5, 1.5, 2) calculated against `screenWidth`, totaling ~900px. Combined with identity and actions rows, total exceeds screen width → overflow/overlap. Fix: Restructure card as **2 stacked rows** instead of 1 wide row. Row 1: icon + name/sqft + actions. Row 2: address · city · frequency badge · geofence status (metadata row with dot separators, matching Safegroup pattern). | widget-rules: Rule 22 added (avoid nested rows with grid-width children) |
| 2026-03-12 | EFS | facilities_list | Geofence "circle" icon renders as hollow outline ring (○) not a filled dot | Feather `circle` icon is an outline/stroke icon. For a filled status dot indicator, use a **text widget** with Unicode filled circle `"●"` (U+25CF) and `font_size: 8`. Supports `color_key` + `color_map` for dynamic coloring. Alternative: tiny container with `border_radius: 50` and `color` but this requires fixed dimensions which don't work well in FieldProxy. | widget-rules: Rule 23 added (filled dot indicators) |
| 2026-03-12 | EFS | facilities_list | KPI stat cards (grouped_counter_1) not rendering — blank space between header and list | Missing required properties: `width`, `columns`, `element_count`, `isBackground`, `customAspectRatio`. Compare with working supply_requests version which has all 5 properties. The `cards` array alone is insufficient. Fix: Added `"width": 12, "columns": 4, "element_count": 4, "isBackground": true, "customAspectRatio": 1.3`. | widget-rules: Rule 24 added (grouped_counter_1 required properties) |
| 2026-03-12 | EFS | facilities_list | Popup content overflows off-screen to the right — 3-column rows (width:4 each) extend beyond 700px popup boundary | `calculateDynamicWidth()` ALWAYS uses `screenWidth`, even inside popups. `width: 4` = `4/12 × 1440px = 480px`. Three columns = 1440px inside a 700px popup → massive overflow. Formula: max usable grid columns inside popup = `popupWidth / screenWidth × 12`. For 700px popup on 1440px screen: ~5.8 columns. Fix: For 3-column popup rows, use `width: 2` per column (2/12 × 1440 = 240px × 3 = 720px ≈ fits). For 2-column popup rows, use `width: 3` per column. | widget-rules: Rule 25 added (popup grid width calculation) |
| 2026-03-14 | Safegroup | create_visit_job_form | `visibility_condition` on `type: "input"` widgets is silently ignored — ASHP/MPHW equipment fields (pf_ashp_make_model, pf_ashp_serial, pf_cylinder_make_model, pf_cylinder_serial) visible regardless of selected type | `visibility_condition` works on **containers**, **text**, **buttons**, **space_widget** — but NOT on `type: "input"` widgets directly. The property is silently ignored; the input always renders. Fix: Wrap each conditionally-visible input (or group of inputs) in a `container` with the `visibility_condition` on the container, not the input. | widget-rules: Rule 26 added (visibility_condition on inputs) |
| 2026-03-14 | Safegroup | create_visit_job_form | List inside a container with `height: 280` does NOT scroll — items just overflow or get clipped | Container `height` constrains the container's box but does NOT propagate scrollability to child widgets. A `type: "list"` only becomes scrollable when `height` is set **directly on the list widget itself**. Fix: Removed `height` from parent container, added `"height": 200` directly on the `type: "list"` widget. | widget-rules: Rule 27 added (list height for scrolling) |
| 2026-03-14 | Safegroup | create_visit_job_form | Used `cross_axis_alignment: "stretch"` on row to match side-by-side card heights — user rejected; shorter card stretched unnaturally | `"stretch"` forces both cards to the tallest card's height, but the stretched card looks wrong with empty white space distributed unpredictably. User prefers `"start"` (each card takes natural height) + manual `padding_bottom` on the shorter card to visually match heights. Fix: Reverted to `"start"`, added `padding_bottom: 105` to Customer card. | widget-rules: Rule 28 added (side-by-side card height matching) |
| 2026-03-14 | Safegroup | create_visit_job_form | Initial `padding_bottom: 60` insufficient to match side-by-side card heights — visible gap remained | Estimated padding too low. User manually tested and confirmed `padding_bottom: 105` as the correct value. **Lesson:** When matching card heights manually, start with a larger value (80-120px) and let the user fine-tune, rather than guessing conservatively. | afrin-design: padding estimation guidance |
| 2026-03-14 | Safegroup | create_visit_job_form | Empty state "No visits scheduled" card used grey theme (#FAFAFA bg, #E5E7EB border, #9CA3AF text) — user wanted blue | Grey empty states look dull and disconnected from the app's brand. User specified blue theme: `#EFF6FF` bg, `#DBEAFE` border, `#2563EB` icon, `#1E40AF` title, `#3B82F6` subtitle. Fix: Changed all grey colors to blue equivalents. | afrin-design: empty state blue theme pattern added |
| 2026-03-15 | Safegroup | visit_detail_web | Horizontal divider lines (1px containers used as separators) not rendering — invisible despite correct color/width | `fixed_height` is NOT a valid FieldProxy container property — it is silently ignored, so the container collapses to 0px height. The correct property is `height`. Fix: Changed all `"fixed_height": 1` to `"height": 1` across all divider containers (5 instances). | widget-rules: Rule 29 added (container height property) |
| 2026-03-15 | Safegroup | visit_detail_web | `space_widget` with `alignment: "horizontal"` used as replacement for `gutter` between row children — renders no spacing, columns overlap | `type: "space_widget"` does NOT work as a horizontal spacer between row children. Only `type: "gutter"` creates proper horizontal gap between row children (~0.5 grid units). `space_widget` with `alignment: "horizontal"` works inside a row for inline icon-to-text spacing, but NOT as a structural gap between major row children (columns/containers). Fix: Reverted to `type: "gutter"`. Adjusted column widths (left 7.5, right 4.0) instead to reduce visual gap. | widget-rules: Rule 30 added (gutter vs space_widget between row children) |
| 2026-03-15 | Safegroup | visit_detail_web | Vertical connecting line (1px container, width 0.01) inside a column not centering — stays left-aligned despite `cross_axis_alignment: "center"` on parent column | Column `cross_axis_alignment: "center"` does NOT reliably center very thin containers (`width: 0.01`). The grid width calculation `0.01/12 × screenWidth` gives ~1.2px — too small for the layout engine to position accurately. `main_axis_size: "min"` on the column also didn't help. Fix: Removed the connecting line entirely. FieldProxy's grid system is not designed for sub-pixel positioning. | nandy-layout: thin container centering limitation |
| 2026-03-15 | Safegroup | visit_detail_web | "View PDF" button overflowing right edge of narrow container (4.0 width card with 16px padding) — button text + icon clipped | **Three compounding issues:** (1) `spacer` between text and button in a row pushed button to far-right edge with no room. (2) `outlined_button` with icon (`external-link`) + text (`View PDF`) was too wide for available space. (3) No `width` constraint on button — it expanded to its natural size. **Fix sequence:** Removed icon → still overflowed. Changed to `filled_button` → still overflowed. Changed row to column layout (stacked vertically) → still overflowed. Finally added `width: 2` to button — resolved. **Key learning:** Buttons DO support `width` property (confirmed in quote_detail_view.json: `width: 1.5`, `width: 2`, `width: 2.5`). In narrow containers, ALWAYS set explicit `width` on buttons. | widget-rules: Rule 31 added (button width in narrow containers) |
| 2026-03-15 | Safegroup | visit_detail_web | "View PDF" button used `open_url` action to navigate directly to PDF URL — user wanted inline PDF viewing with popup | `open_url` navigates away from the current page. For document viewing, the correct pattern is: button `onclick` → `type: "popup"` (fullscreen) → container (width: 800, white, padded) with header row (title text + spacer + Download outlined_button) + PDF viewer widget (`type: "pdf"`, `pdf_type: "url"`, `height: 800`). Download button uses `navigation` with `navigation_type: "url"` to open in new tab. | afrin-design: PDF viewer popup pattern added |
| 2026-03-16 | Safegroup | quote_detail | `"type": "divider"` not rendering — no visible line between T&C rows | `"type": "divider"` is NOT a supported widget type in FieldProxy web renderer. Use container-based divider pattern instead: `{"type": "container", "color": "#E5E7EB", "width": 12, "height": 1, "element_id": "..."}`. This is the same pattern used in visit_detail_web for horizontal separators. | widget-rules: divider must use container pattern |
| 2026-03-16 | Safegroup | quote_detail | `cross_axis_alignment: "stretch"` on row causes shorter card (Activity Log) to stretch unnaturally with empty white space | Per Rule 28 (from 2026-03-14): `stretch` distorts shorter cards. Use `"start"` + manual `padding_bottom` on the shorter card. For quote_detail: Activity Log card got `padding_bottom: 180` to visually match T&C card height. Start higher (200-300), let user fine-tune down. | widget-rules: Rule 28 (repeat) |
| 2026-03-16 | Safegroup | quote_detail | Activity Log list item had white box artifact — `border_color`/`border_width` on transparent container creates visible white bordered rectangle | When `aud_item_wrapper` has `color: "transparent"` but `border_color: "#F3F4F6"` + `border_width: 1`, the border draws a visible white-ish rectangle against the page background. Fix: Remove `border_color` and `border_width`, use only `color: "#F9FAFB"` (subtle grey fill) with `border_radius` for the card shape. Borders on transparent containers are visually jarring. | widget-rules: avoid borders on transparent containers |
| 2026-03-15 | Safegroup | visit_detail_web | Height matching between left and right columns — initial height estimates for Activity Timeline and Checklist Progress were incorrect | Activity Timeline needed to match Header + Customer & Site cards height. Initial 620px → user corrected to exactly 623px. Checklist Progress needed to match Job Details card. Initial 600px → too short, visible gap at bottom → increased to 830px which matched. **Key learning:** When using explicit `height` for cross-column alignment, start higher and let user fine-tune down. Two separate approaches exist: (1) `padding_bottom` for simple height matching, (2) explicit `height` property when card content is dynamic (lists, conditional sections) and padding alone won't work. | nandy-layout: height matching with explicit height property |
| 2026-03-17 | Safegroup | enquiry_form_view | "Assign To" dropdown renders as broken search widget with "Null check operator used on a null value" | **Two compounding issues:** (1) `fp_users` query used `username` column and `WHERE is_active = TRUE` — `fp_users` system table uses `name` (not `username`) and has no `is_active` column → query fails, data returns null → null check error. Fix: `SELECT id, name FROM fp_users ORDER BY name ASC`. (2) Builder incorrectly changed dropdown from `show`/`store` pattern to `value_field`/`display_field` — for `fp_users` dropdowns, the correct pattern is `"show": "[name]", "store": ["name"], "show_key": false`. `value_field`/`display_field` works for entity dropdowns (customers, statuses) but `show`/`store` is the correct pattern for user selection dropdowns. | widget-rules: fp_users query columns + show/store vs value_field/display_field |
| 2026-03-18 | Safegroup | enquiries_view | **Two issues:** (1) Apply/Clear buttons rendered as huge grey block — missing `width` property. `filled_button` without `width` defaults to 12 (full screen width). Fix: Added `width: 1` to both. (2) Total stat card (16) didn't match list count (15) — `enquiries_total` query counted ALL enquiries including archived, but list excluded archived. Closed stat card onclick also included ARCHIVED. Fix: Added archived exclusion to `enquiries_total` data query, Total stat card onclick, Closed stat card onclick, Apply button total update, and search query. | widget-rules: filled_button always needs explicit width |
| 2026-03-18 | Safegroup | enquiries_view | Clear button used `refresh_data` which reloads entire page — loses user context, resets all dropdown/filter selections, flashes the UI | `refresh_data` is a full page reload. For "Clear filter" buttons, use `update_data` with the **default** (unfiltered) query for each data key instead. This resets the data without losing page state. The Clear button should mirror the Apply button's onclick structure (same 8 `update_data` steps) but with queries from the `data` section (no date range filter). | widget-rules: Rule 30 updated — refresh_data loses page state |

| 2026-03-19 | Safegroup | create_quote_view | `default_value` on inputs does NOT pre-fill on standalone page views — dropdown and text fields remain empty | `default_value` only works inside **popups** (where parent view data is already loaded). For standalone page views, the correct property is **`init`** — e.g., `"init": "{{prefill_data.data.[0].field_name}}"`. `init` is used across 63+ files in the codebase for pre-filling form inputs from data queries. Pattern: add a `prefill` data query in the `data` section, then reference it via `init` on each input. | widget-rules: Rule 33 added (init vs default_value) |
| 2026-03-20 | Safegroup | enquiry_detail | Quote status badges not rendering — CASE WHEN query returns correct status_color/status_label values but containers with `"color": "{{quotes.data.[index].status_color}}"` render as invisible/white | **Dynamic template values in container `color` property do NOT work inside list items.** Attempted 4 approaches: (1) `CASE WHEN qs.code IN ('DRAFT',...) THEN '#...'` → invisible. (2) `LOWER(qs.name)` matching → invisible. (3) Simple `CASE COALESCE(qs.code,'') WHEN 'DRAFT' THEN '#...'` → data correct but still invisible. (4) **Separate containers with `visibility_condition` per status** → WORKS. The template `{{...status_color}}` resolves but the container renderer doesn't repaint with the resolved color. **Fix**: Use the **visibility_condition pattern** — one container per status with hardcoded static `color` and `visibility_condition: "'{{...status_code}}' == 'DRAFT'"`. Same pattern already proven in Visits & Jobs tab for visit status badges. | widget-rules: Rule 39 added (status badges in lists) |

---

## Pending Investigations

<!-- Add unresolved issues here. Move to the log table once diagnosed. -->

| Date | Project | View | Symptom | Status |
|------|---------|------|---------|--------|
| 2026-03-20 | Safegroup | edit_enquiry_view | Input fields overflow container when child widths (e.g. 5.5+5.5=11) calculated against screenWidth but rendered inside a width:8.5 container — inputs extend past card border | Fixed: `calculateDynamicWidth()` ALWAYS uses screenWidth, never parent width. Two inputs at width:6 inside a width:8.5 container = 100% of screen inside 70% of screen = overflow. **Fix**: use width:4 per column (33% screen = ~47% of 8.5 container). For full-width fields use width:8 (66% screen = ~95% of container). **Rule**: parent_grid_width x 2 / 3 = max safe child width for 2-column layouts |
| 2026-03-20 | Safegroup | edit_enquiry_view | Dropdown init with display name causes onchange to pass name instead of ID — "Processing" spinner, SQL type mismatch error | Fixed: when dropdown has `value_field: "id"`, `init` must be the ID or omitted entirely. Setting init to a display name (e.g. "Proxima GR Properties") causes FieldProxy to store that string as the value. Onchange queries like `WHERE id = 'Proxima GR...'` fail. **Fix**: don't prefill dropdowns on edit screens — show current values in a separate read-only panel instead |
| 2026-03-20 | Safegroup | edit_enquiry_view | Popup `is_scrollable: true` and `fullscreen: true` both fail to make popup content scrollable | Fixed: from source code — regular popups use `Column(mainAxisSize: min)` with no scroll wrapper. `fullscreen: true` uses `SingleChildScrollView` but still overflows in practice. **Fix**: move edit forms to a separate view (navigation) instead of popup. Views scroll naturally |
| 2026-03-20 | Safegroup | edit_enquiry_view | `default_value` property on inputs does nothing — fields show empty | Fixed: `default_value` is not a valid FieldProxy property. Use `init` for prefilling text inputs and dropdowns |
| 2026-03-20 | Safegroup | edit_enquiry_view | `contacts` table JOIN with alias `ct.first_name` throws "column does not exist" | Fixed: alias conflict in PostgreSQL. Use subquery `(SELECT first_name FROM contacts WHERE id = e.contact_id)` instead of JOIN. Or verify table columns match the actual schema before writing JOINs |
| 2026-03-22 | EFS | all cleaner app screens | `{{global.user_id}}` does NOT resolve — queries return empty/null, user-specific data never loads | **`{{global.user_id}}` is NOT a valid FieldProxy system variable.** The correct syntax for getting the current user ID is `'<!getUserId()!>'` (with single quotes and `<!...!>` delimiters). Only 3 system functions exist: `<!getUserId()!>`, `<!getLocation().latitude!>`, `<!getLocation().longitude!>`. See `project-root/logical elements/system_variables.md` for full reference. **Fix**: Replace all `{{global.user_id}}` with `'<!getUserId()!>'` in queries. Pattern: `WHERE requested_by = '<!getUserId()!>'` |
| 2026-03-25 | EFS | create_checklist_view | MCQ shows raw `{label, value}` objects instead of clean text | Static MCQ data must be plain string arrays `["Monday", "Tuesday"]`, not `{label, value}` objects. Also need `is_multi: true` for checkboxes | widget-rules: Rule 42 added |
| 2026-03-25 | EFS | create_checklist_view | MCQ shows IDs (1, 2, 3) instead of names for query-driven data | MCQ query should return a single column: `SELECT name FROM table` — not `SELECT id AS value, name AS label` | widget-rules: Rule 42 added |
| 2026-03-25 | EFS | create_checklist_view | Edit popup always pre-fills with first row's data regardless of which row's edit button clicked | `init` evaluates `[index]` at render time (always 0). Use `update_data` in onclick to fetch row by ID first, then `init` references `[0]` on fetched data | widget-rules: Rule 41 added |
| 2026-03-25 | EFS | create_checklist_view | `update_input_value` and `select` used as onclick actions — invalid action types for pre-filling popup inputs | These are NOT valid onclick action types for setting input values. `update_input_value` is only for `onchange` handlers. `select` only works for filter dropdowns on the main page, not popup inputs | error-log only |
| 2026-03-25 | EFS | create_checklist_view | Weekly days stored with `{}` braces — shows `{Wednesday, Friday}` in list | MCQ `formatted_value` stores arrays with braces. Strip in queries: `REPLACE(REPLACE(col, '{', ''), '}', '')` | widget-rules: Rule 42 added |
| 2026-03-25 | EFS | checklists_list_view | Last list card stretches to fill entire viewport — massive empty space inside card | List without `height` property causes FieldProxy flex layout to stretch last item. Fix: add `height: 700` to list widget | widget-rules: Rule 40 added |
| 2026-03-25 | EFS | create_checklist_view | `default_value` used for popup input pre-fill — confirmed NOT valid | Repeat of 2026-03-20 finding. `default_value` is NOT a valid property. Use `init` only | error-log only |
| 2026-03-25 | EFS | create_checklist_view | `color_key`/`color_map` on badges in list shows wrong colors | Dynamic color via `color_key`/`color_map` can fail inside list items (Rule 39). Use separate containers with `visibility_condition` for each status/frequency value | widget-rules: Rule 39 confirmed |
| 2026-03-25 | EFS | all web views | `fp_users` table column is `username` not `name` in EFS project | Different projects may have different `fp_users` schemas. EFS uses `username`. Always check existing queries in the project before assuming column names | error-log only |
| | | | | |
